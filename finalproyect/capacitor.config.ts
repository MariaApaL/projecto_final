import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.maria.apa.cat',
  appName: 'GOut',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
